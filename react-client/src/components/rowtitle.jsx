import React from 'react';
import Topten from './topTen.jsx';

class Row extends React.Component {
    constructor(props) {
        super(props);
    }
    render() {
        return (
            {
                // this.state.title.map(title => <Topten title={title}/>)
            }
        )
    }
}
export default Row;